/// Cafe Vibe — 디자인 시스템 치수 상수
/// 모든 화면에서 일관된 간격, 크기, 반경을 보장합니다.
class AppDimens {
  AppDimens._();

  // ── 터치 타겟 (Material 3 가이드라인: 최소 44pt) ──────────────
  static const double minTouchTarget = 44.0;
  static const double iconButtonSize = 44.0;
  static const double fabSize = 56.0;

  // ── 아이콘 크기 ───────────────────────────────────────────────
  static const double iconXs = 14.0;
  static const double iconSm = 16.0;
  static const double iconMd = 20.0;
  static const double iconLg = 24.0;
  static const double iconXl = 32.0;

  // ── 간격 (spacing) ────────────────────────────────────────────
  static const double spaceXxs = 2.0;
  static const double spaceXs = 4.0;
  static const double spaceSm = 8.0;
  static const double spaceMd = 12.0;
  static const double spaceLg = 16.0;
  static const double spaceXl = 20.0;
  static const double spaceXxl = 24.0;
  static const double spaceHuge = 32.0;

  // ── 카드 & 컨테이너 ──────────────────────────────────────────
  static const double cardRadius = 16.0;
  static const double cardPadding = 16.0;
  static const double cardMarginH = 16.0;
  static const double sheetRadius = 20.0;
  static const double chipRadius = 20.0;
  static const double buttonRadius = 14.0;
  static const double inputRadius = 12.0;

  // ── 바텀시트 ──────────────────────────────────────────────────
  static const double sheetHandleWidth = 40.0;
  static const double sheetHandleHeight = 4.0;
  static const double sheetInitialSize = 0.85;
  static const double sheetMaxSize = 0.95;

  // ── 폰트 크기 ─────────────────────────────────────────────────
  static const double fontCaption = 11.0;
  static const double fontSmall = 12.0;
  static const double fontBody = 14.0;
  static const double fontSubtitle = 15.0;
  static const double fontTitle = 17.0;
  static const double fontHeadline = 18.0;
  static const double fontDisplay = 24.0;

  // ── 그림자 (boxShadow) ───────────────────────────────────────
  static const double shadowBlurSm = 4.0;
  static const double shadowBlurMd = 8.0;
  static const double shadowBlurLg = 12.0;
  static const double shadowOpacity = 0.04;

  // ── Hero AppBar ──────────────────────────────────────────────
  static const double heroExpandedHeight = 200.0;
  static const double stickyButtonHeight = 50.0;
}
